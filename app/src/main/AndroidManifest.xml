<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.android.musicplayer">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:largeHeap="true"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MusicActivity" />
        <activity android:name=".RadioActivity" />
        <activity
            android:name=".AlbumMusicActivity"
            android:label="@string/title_activity_album_music"
            android:theme="@style/AppTheme" />
        <activity android:name=".SongMusicActivity" />
        <activity android:name=".ArtistMusicActivity" />
        <activity android:name=".TrendingRadioActivity" />
        <activity android:name=".PlayingNowRadioActivity" />
        <activity android:name=".LocalRadioActivity" />
        <activity android:name=".GlobalRadioActivity" />
        <activity android:name=".PlayingNowMusicActivity"/>
    </application>

</manifest>