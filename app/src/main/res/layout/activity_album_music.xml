<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    tools:context="com.example.android.musicplayer.AlbumMusicActivity">

    <TextView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:gravity="center"
        android:text="@string/music_albums_label"
        android:textSize="42sp" />

    <TextView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:gravity="center"
        android:text="Activity contains user music sorted by album.
First time on this screen, if there is no local music directory
defined from previous use, an explorer will be opened with an
intent in order to select files/directories. After that, the
selected directories will be scanned and their metadata will
be stored locally by the application. Using this metadata they
will be grouped by album and then each album will be placed
as an element of a gridview." />

</LinearLayout>
